.form-section
  = f.text_area :name, label: 'Section Name' # TODO change to show section number
  = f.check_box :_destroy, label: 'Remove Section'
  = f.fields_for :observation_questions do |question_f|
    = render 'observation_question_fields', f: question_f
